<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpruePad"
        tools:targetApi="31">
        <activity android:name=".ui.SignUpActivity"/>
        <activity android:name=".ui.mainmenu.MainMenuActivity" />
        <activity android:name=".ui.mainmenu.CreateProjectActivity" />
        <activity android:name=".ui.projectcontents.ProjectContentsActivity" />
        <activity android:name=".ui.projectcontents.fragments.paint.CreateColorPickerActivity" />
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>